# Workspace Architecture Overhaul Baseline

Captured on 2026-04-19 before Phase 1A implementation.

## Complexity Metrics

```text
889 apps/web/app/(dashboard)/projects/[projectId]/page.tsx
240 apps/web/hooks/useProjectWorkspaceUi.ts
574 apps/web/components/projects/ProjectChatPanel.tsx
433 apps/web/components/workbench/WorkbenchRightPanel.tsx
775
```

The final value is the count of lines in
`apps/web/app/(dashboard)/projects/[projectId]/page.tsx` that begin with two
spaces.

## Baseline Verification

Command run:

```text
cd apps/web && bun test hooks lib components
```

Result:

```text
800 pass
0 fail
2037 expect() calls
Ran 800 tests across 143 files. [27.52s]
```
